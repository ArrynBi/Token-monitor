macOS build is not performed on this Windows machine.

On a Mac, run:
1. python3 -m pip install -r requirements.txt pyinstaller
2. chmod +x build_macos.sh
3. ./build_macos.sh

Expected output:
- dist/Token悬浮球.app
- release/token-monitor-macos.zip

The macOS icon is now generated automatically:
- token_orb.svg -> build/token_orb.iconset -> src/token_monitor/assets/token_orb.icns
